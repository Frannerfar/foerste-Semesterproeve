<Window x:Class="FoersteSemesterproeve.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FoersteSemesterproeve"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" MinWidth="800" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        
        
        <DockPanel Grid.Row="0" Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Margin="0">
                <Grid x:Name="MenuGrid" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
                        <Button x:Name="HomeButton" Background="LightSlateGray" Content="Home" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Home_Click" />
                        <Button x:Name="ActivitiesButton" Background="LightSlateGray" Content="Activities" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Activities_Click" />
                        <Button x:Name="TrainersButton" Background="LightSlateGray" Content="Trainers" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Trainers_Click" />
                        <Button x:Name="LocationsButton" Background="LightSlateGray" Content="Locations" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Locations_Click" />
                        <Button x:Name="MembersButton" Background="LightSlateGray" Content="Members" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Members_Click" />
                        <Button x:Name="MembershipsButton" Background="LightSlateGray" Content="Memberships" Margin="0" FontSize="18" Height="80" MinWidth="100" Visibility="Visible" Cursor="Hand" BorderThickness="0" Padding="8,4" Click="Memberships_Click" />
                    </StackPanel>

                    <Border Grid.Row="0" Grid.Column="1" Background="LightSlateGray">
                        
                    </Border>
                    
                    <Border Width="100" Grid.Row="0" Grid.Column="2">
                        <StackPanel Orientation="Vertical">
                            <Button x:Name="UserProfileButton" Click="Profile_Click" Cursor="Hand">
                                <Image Source="/Data/Assets/Profile.png" Height="56" Width="35" />
                            </Button>
                            <Button Click="LogOut_Click" Cursor="Hand">Log out</Button>
                        </StackPanel>
                    </Border>            
                </Grid>
            </StackPanel>
        </DockPanel>
        <ContentControl Grid.Row="1" Grid.Column="0" x:Name="MainContent" />
    </Grid>
</Window>
