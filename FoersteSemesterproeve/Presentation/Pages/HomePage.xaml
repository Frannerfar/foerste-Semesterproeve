<UserControl x:Class="FoersteSemesterproeve.Presentation.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoersteSemesterproeve.Presentation.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="*" />

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock x:Name="AuthenticatedUserName" Margin="20 20" HorizontalAlignment="Center"  FontSize="30">Hello</TextBlock>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <Border BorderThickness="1" BorderBrush="LightGray" Padding="50 10 50 10" CornerRadius="5">
                    <Label FontSize="18">Your upcoming Activities</Label>
                </Border>
            </StackPanel>

            <ScrollViewer Grid.Row="2"  Grid.Column="0" VerticalScrollBarVisibility="Auto">
                
                <Grid x:Name="UpcomingActivitiesGrid" MaxWidth="800">
                
                </Grid>
                
            </ScrollViewer>
        </Grid>
        
    </Grid>

</UserControl>
