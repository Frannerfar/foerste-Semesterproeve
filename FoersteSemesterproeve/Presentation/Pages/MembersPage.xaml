<UserControl x:Class="FoersteSemesterproeve.Presentation.Pages.MembersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoersteSemesterproeve.Presentation.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" MaxWidth="1000" Margin="10 20 10 10">
            <Button Click="AddUser_Click" Width="auto" Padding="20 0" Height="30" FontSize="18" Background="#FF6A9DCA" Cursor="Hand" HorizontalAlignment="Right">Add User</Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" MaxWidth="1000" Margin="10">
            <DataGrid x:Name="MemberDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>

                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}" MinWidth="50"  />
                    <DataGridTextColumn Header="First Name" Binding="{Binding firstName}" MinWidth="100" MaxWidth="150" Width="*"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding lastName}" MinWidth="100" MaxWidth="150" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding email}" MinWidth="100" Width="*"/>
                    <DataGridTextColumn Header="Membership" Binding="{Binding membershipType.name}" MinWidth="100"/>
                    <DataGridTextColumn Header="Trainer" Binding="{Binding isCoachText}" MinWidth="50"/>
                    <DataGridTextColumn Header="Admin" Binding="{Binding isAdminText}" MinWidth="50"/>

                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                        Margin="5"
                                        Padding="10 5"
                                        Tag="{Binding}"
                                        Background="LawnGreen"
                                        Cursor="Hand"
                                        Click="EditUser_Click"/>

                                    <Button Content="Delete"
                                        Margin="5"
                                        Padding="10 5"
                                        Background="Red"
                                        Cursor="Hand"
                                        Tag="{Binding}"
                                        Click="DeleteUser_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0">
            
            
        </StackPanel>


    </Grid>
</UserControl>
