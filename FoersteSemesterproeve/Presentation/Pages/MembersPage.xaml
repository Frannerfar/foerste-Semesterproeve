<UserControl x:Class="FoersteSemesterproeve.Presentation.Pages.MembersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoersteSemesterproeve.Presentation.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" MaxWidth="1000" Margin="10">
            <Button Click="AddUser_Click" Width="30" Height="30" FontSize="20" Background="AliceBlue" HorizontalAlignment="Right">+</Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" MaxWidth="1000" Margin="10">
            <DataGrid x:Name="MemberDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding id}"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding firstName}"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding lastName}"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding email}"/>
                    <DataGridTextColumn Header="Trainer" Binding="{Binding isCoachText}"/>
                    <DataGridTextColumn Header="Admin" Binding="{Binding isAdminText}"/>

                    <DataGridTemplateColumn Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit"
                                        Margin="5"
                                        Padding="5"
                                        Tag="{Binding}"
                                        Click="EditUser_Click"/>

                                    <Button Content="Delete"
                                        Margin="5"
                                        Padding="5"
                                        Tag="{Binding}"
                                        Click="DeleteUser_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0">
            
            
        </StackPanel>


    </Grid>
</UserControl>
