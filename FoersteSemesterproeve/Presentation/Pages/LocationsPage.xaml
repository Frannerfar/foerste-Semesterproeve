<UserControl x:Class="FoersteSemesterproeve.Presentation.Pages.LocationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoersteSemesterproeve.Presentation.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" MaxWidth="1000" Margin="10 20 10 10">
            <Button x:Name="AddLocationButton"
                Grid.Column="1"
                Content="Add Location"
                Click="AddLocationButton_Click"  
                Width="auto" 
                Padding="20 0" 
                Height="30" 
                FontSize="18" 
                Background="#FF6A9DCA" 
                Cursor="Hand" 
                HorizontalAlignment="Right"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Grid.Column="0" MaxWidth="1000">
            <Grid x:Name="GridLocations" MaxWidth="1000" Margin="10">
            </Grid>
        </ScrollViewer>
        
    </Grid>
</UserControl>
